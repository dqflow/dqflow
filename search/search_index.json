{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"dqflow","text":"<p>Data contracts for Python data pipelines.Define \u2192 Validate \u2192 Fail Fast</p> <p> </p> <p>dqflow lets you declare what a DataFrame must look like \u2014 required columns, valid values, table-level rules \u2014 as a small, versionable contract, validate your data against it inside the pipeline, and stop bad data before it reaches anything downstream.</p> <p>Current enforcement boundary</p> <p><code>dtype</code>, <code>freshness_minutes</code>, and <code>custom</code> can be declared but are not validated by the engines yet. The <code>pattern</code> regex constraint is enforced.</p>"},{"location":"#workflow","title":"Workflow","text":"<pre><code>  1. DEFINE                2. VALIDATE                 3. FAIL FAST\n  \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500                \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500                 \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n  write a contract   \u2500\u25b6    contract.validate(df)  \u2500\u25b6   result.ok is False\n  in Python or YAML        \u2192 ValidationResult          \u2192 raise / exit code \u2260 0\n\n                                                       result.ok is True\n                                                       \u2192 pipeline continues\n</code></pre>"},{"location":"#30-second-quick-start","title":"30-second quick start","text":"<pre><code>import pandas as pd\nfrom dqflow import Contract, Column\n\ndf = pd.DataFrame({\n    \"order_id\": [\"A001\", \"A002\", None, \"A004\"],\n    \"amount\":   [19.99, -5.00, 42.50, 99.00],\n    \"currency\": [\"USD\", \"EUR\", \"USD\", \"GBP\"],\n})\n\ncontract = Contract(\n    name=\"orders\",\n    columns={\n        \"order_id\": Column(str, not_null=True, unique=True),\n        \"amount\":   Column(float, min=0),\n        \"currency\": Column(str, allowed=[\"USD\", \"EUR\"]),\n    },\n    rules=[\"row_count &gt; 0\"],\n)\n\nresult = contract.validate(df)\nprint(result.summary())\n\nif not result.ok:\n    raise ValueError(result.summary())\n</code></pre> <pre><code>Contract 'orders': 5/8 checks passed\nFailed checks:\n  - not_null:order_id: Column 'order_id' has 1 null value\n  - min:amount: Column 'amount' has 1 value below the minimum 0\n  - allowed:currency: Column 'currency' has 1 value outside the allowed set\n</code></pre>"},{"location":"#why-dqflow","title":"Why dqflow","text":"<ul> <li>Contracts, not scattered asserts. One declarative artifact \u2014 reviewed, diffed,   and versioned like the rest of your code.</li> <li>Lightweight. Three runtime dependencies (<code>pandas</code>, <code>pyyaml</code>, <code>click</code>). No   server, no database, no daemon.</li> <li>Pythonic. Plain <code>Contract</code> / <code>Column</code> objects, or YAML. Validation returns a   structured result object, not a stack trace.</li> <li>Fail fast, on purpose. <code>result.ok</code> is a boolean; <code>dq validate --fail-fast</code>   evaluates every check and returns a non-zero exit code on validation failure.</li> </ul>"},{"location":"#whats-implemented","title":"What's implemented","text":"Capability Status Python &amp; YAML contracts \u2705 Implemented Schema check \u2014 required columns must be present \u2705 Implemented Validity checks \u2014 <code>not_null</code>, <code>min</code>, <code>max</code>, <code>allowed</code>, <code>unique</code>, <code>pattern</code> \u2705 Implemented Table rules \u2014 <code>row_count</code>, <code>null_rate('col')</code>, <code>unique_count('col')</code> \u2705 Implemented Cross-column rules \u2014 <code>left</code>/<code>op</code>/<code>right</code> or a callable \u2705 Implemented Structured results \u2014 <code>.ok</code>, <code>.failed_checks</code>, <code>.summary()</code>, <code>.to_dict()</code> \u2705 Implemented CLI \u2014 <code>dq validate</code> / <code>dq show</code> / <code>dq infer</code> / <code>dq diff</code> \u2705 Implemented Contract diff \u2014 breaking / non-breaking classification, JSON, CI exit code \u2705 Implemented pandas engine \u2705 Implemented Polars engine (<code>dqflow[polars]</code>) \ud83e\uddea Experimental Declared <code>dtype</code> / <code>freshness_minutes</code> / <code>custom</code> enforcement \ud83d\udd1c Not yet enforced PySpark &amp; SQL engines, GitHub Action, HTML reports \ud83d\udd1c Planned <p>Note</p> <p>A <code>Column</code> accepts <code>dtype</code>, <code>freshness_minutes</code>, and <code>custom</code> today, and <code>dq show</code> / <code>dq infer</code> use the declared dtype \u2014 but the engines do not yet validate data against them. Regex <code>pattern</code> constraints are enforced. See the roadmap.</p>"},{"location":"#when-to-use-dqflow","title":"When to use dqflow","text":"<ul> <li>You have Python data pipelines \u2014 Airflow, Dagster, Prefect, dbt-adjacent scripts,   notebooks headed for production.</li> <li>You want data expectations in git, reviewed in pull requests.</li> <li>You want a pipeline step or CI job to hard-fail on bad data.</li> <li>Your data fits in memory as a pandas (or Polars) DataFrame.</li> </ul>"},{"location":"#when-not-to-use-dqflow","title":"When not to use dqflow","text":"<ul> <li>You need dashboards, alerting, anomaly detection, or lineage \u2014 dqflow produces   files and exit codes, not a web app.</li> <li>You need warehouse push-down or Spark-scale distributed validation (planned, not   available).</li> <li>You need dtype / freshness / <code>custom</code> enforcement today. Regex <code>pattern</code>   constraints are supported.</li> <li>You need to execute contracts from untrusted sources. Table-rule expressions   are evaluated by a whitelisted AST walker (no <code>eval</code>), but that is still not a   security boundary \u2014 run only contracts you trust.</li> </ul> <p>Quote</p> <p>dqflow is not a full data observability platform. It is a small, opinionated library meant to be embedded directly into pipelines. Where richer tooling is useful, dqflow's job is to emit clean, structured results those systems can consume.</p>"},{"location":"#next-steps","title":"Next steps","text":"<ul> <li>Installation</li> <li>Quick Start</li> <li>Defining Contracts</li> <li>YAML Contracts</li> <li>CLI Usage</li> <li>Diffing Contracts</li> <li>Validate in an ETL Pipeline</li> <li>Gate a Pull Request</li> <li>Roadmap</li> </ul>"},{"location":"changelog/","title":"Changelog","text":"<p>The complete release history lives in <code>CHANGELOG.md</code>.</p>"},{"location":"changelog/#unreleased","title":"Unreleased","text":"<ul> <li><code>dq lint CONTRACT</code> \u2014 validate a contract file without reading data. Path- and   line-aware diagnostics for unknown fields, wrong types, bad regexes,   contradictory bounds, unparseable rules, and an unsupported schema version;   <code>--output json</code>, <code>--strict</code>, exit <code>1</code> on errors   (#61).</li> <li>Contracts carry a <code>schema_version</code> (<code>\"1.0\"</code>), always written by <code>to_yaml()</code> /   <code>dq infer</code>; a <code>MAJOR.MINOR</code> compatibility policy governs cross-version loading.   See Schema versioning.</li> <li><code>Contract.from_yaml()</code> validates before construction and raises a typed   <code>dqflow.schema.ContractError</code> (<code>ContractParseError</code> / <code>ContractSchemaError</code> /   <code>ContractVersionError</code>) instead of a traceback; unknown fields are rejected.</li> <li><code>dqflow.schema</code> \u2014 <code>lint_contract_file</code> / <code>lint_contract_data</code>, <code>Diagnostic</code>,   and the error classes.</li> <li>A published JSON Schema for the contract format   (<code>https://dqflow.github.io/dqflow/schema/contract-1.0.json</code>, <code>dq schema</code>,   <code>dqflow.schema.contract_json_schema()</code>) for editor autocompletion \u2014 see   Editor integration. A subset of <code>dq lint</code>.</li> <li><code>dqflow.execution.ExecutionContext</code> \u2014 a frozen dataclass carrying a run's   engine, cache toggle, and reserved execution flags   (<code>parallel</code> / <code>max_workers</code> / <code>strict</code> / <code>fail_fast</code>).   <code>Contract.validate(df, context=ExecutionContext(...))</code> is the full form;   <code>engine=</code> remains a shortcut, and <code>dq validate --engine</code> builds the context   (#15).</li> <li><code>ExecutionContext(cache=False)</code> disables memoisation of table-rule statistics   (the <code>StatsCache</code> on/off switch deferred to #15).</li> <li><code>ExecutionContext</code>, <code>Engine</code>, <code>ValidationSpec</code>, <code>StatsCache</code>, <code>evaluate_rule</code>,   <code>get_engine</code>, <code>register_engine</code> and <code>available_engines</code> are now importable   straight from <code>dqflow</code>.</li> <li><code>Engine.validate()</code> now takes a keyword-only <code>context</code> argument instead of   <code>**kwargs</code>; the old no-op <code>parallel</code> / <code>max_workers</code> keywords are gone (pass an   <code>ExecutionContext</code> instead).</li> </ul>"},{"location":"changelog/#040-2026-09-02","title":"0.4.0 \u2014 2026-09-02","text":"<p>The Architecture Foundation refactor. Behaviour and the <code>--output json</code> schema are unchanged (apart from the empty-frame <code>null_rate</code> fix below).</p> <ul> <li>Engine registry (<code>dqflow.engines.get_engine</code> / <code>register_engine</code>).   <code>Contract.validate(df, engine=...)</code> accepts an engine name or instance,   <code>dq validate --engine pandas|polars</code> selects it from the CLI, and <code>Contract</code>   no longer imports an engine (#17).</li> <li><code>dqflow.spec.ValidationSpec</code> \u2014 contracts compile to an engine-agnostic   intermediate representation, and the pandas and Polars engines execute that   spec rather than reading <code>Column</code> objects   (#16).</li> <li><code>dqflow.rules.evaluate_rule</code> \u2014 a single whitelisted-AST evaluator for table   rules shared by every engine; the pandas and Polars engines no longer call   <code>eval</code> (#18).</li> <li><code>dqflow.cache.StatsCache</code> \u2014 a shared, lazy, memoized cache for the statistics   table rules use; each engine supplies three primitives and a column is scanned   at most once (#21).</li> <li>Grouped, colourised <code>dq validate</code> text output with per-check failure rates and   a bounded sample of the offending values, plus <code>--quiet</code> / <code>--verbose</code> /   <code>--color</code> flags and a standalone <code>dqflow.report.render_result()</code> renderer   (#38).</li> <li>Engine <code>message</code> strings now always name the column and the expectation;   failing <code>CheckResult.details</code> gained rate and sample fields. <code>null_rate()</code> on   an empty DataFrame is now <code>0.0</code> (not <code>NaN</code>) in the pandas engine.</li> </ul>"},{"location":"changelog/#030-2026-08-31","title":"0.3.0 \u2014 2026-08-31","text":"<ul> <li>Added <code>dq diff</code> and <code>dqflow.diff.diff_contracts()</code> \u2014 compare two contract   versions and flag every change as breaking or non-breaking for data producers   (text / JSON output, CI exit code, a documented classification table, and a   runnable <code>examples/contract-diff</code> project).</li> <li>Bumped the package version consistently (<code>dq --version</code> was stuck at <code>0.2.1</code>)   and removed stale re-exports from <code>dqflow.engines</code>.</li> </ul>"},{"location":"changelog/#022-2026-08-31","title":"0.2.2 \u2014 2026-08-31","text":""},{"location":"changelog/#added","title":"Added","text":"<ul> <li>Generated API reference for contracts, columns, results, engines, and inference</li> <li>Task-oriented workflow guides and four runnable example projects</li> <li>Strict documentation builds in CI and a contributor setup script</li> <li>Optional <code>parquet</code> extra for CLI Parquet input</li> </ul>"},{"location":"changelog/#fixed","title":"Fixed","text":"<ul> <li>Documentation now distinguishes the enforced <code>pattern</code> check from descriptive   <code>dtype</code>, <code>freshness_minutes</code>, and <code>custom</code> fields</li> <li>Rule syntax, evaluator safety, and <code>--fail-fast</code> behavior are documented   according to the current implementation</li> </ul>"},{"location":"changelog/#021-2026-08-28","title":"0.2.1 \u2014 2026-08-28","text":"<ul> <li>Fixed README and docs rendering on PyPI.</li> <li>Replaced diagrams that did not render consistently.</li> <li>Corrected project links and the PyPI badge.</li> </ul>"},{"location":"changelog/#020-2026-08-28","title":"0.2.0 \u2014 2026-08-28","text":"<ul> <li>Added the experimental Polars engine and cross-column rules.</li> <li>Added <code>unique</code>, <code>pattern</code>, <code>unique_count()</code>, contract inference improvements,   benchmarks, the roadmap, and the redesigned project documentation.</li> <li>Standardized the engine interface and structured output parity.</li> </ul>"},{"location":"contributing/","title":"Contributing","text":"<p>The canonical contributor guide is <code>CONTRIBUTING.md</code>.</p>"},{"location":"contributing/#from-scratch-setup","title":"From-scratch setup","text":"<p>Fork and clone the repository, then on macOS or Linux run:</p> <pre><code>./scripts/setup-dev.sh\nsource .venv/bin/activate\n</code></pre> <p>The script creates an isolated environment, installs development, docs, and Polars dependencies, and installs pre-commit hooks. A manual Windows setup is documented in the canonical guide.</p>"},{"location":"contributing/#verify-a-change","title":"Verify a change","text":"<pre><code>pytest\nruff format --check .\nruff check .\nmypy src/dqflow\nmkdocs build --strict\n</code></pre> <p>Update tests and user documentation with behavior changes. New or changed docs must build without warnings because the same strict build runs in CI.</p>"},{"location":"api/cache/","title":"Statistics cache","text":"<p><code>StatsCache</code> is the engine-agnostic cache behind table-rule statistics \u2014 <code>row_count</code>, <code>null_rate(col)</code>, and <code>unique_count(col)</code>. It is lazy (nothing is computed until a rule asks) and memoized (a column used by several rules is scanned once), and it is scoped to a single <code>validate()</code> call.</p> <p>Each engine provides a subclass with three primitives that read its DataFrame; <code>PandasEngine</code> uses <code>dqflow.engines.pandas.PandasStatsCache</code> and <code>PolarsEngine</code> uses <code>dqflow.engines.polars.PolarsStatsCache</code>.</p>"},{"location":"api/cache/#dqflow.cache.StatsCache","title":"<code>dqflow.cache.StatsCache</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Lazily computed, memoized column statistics for one <code>validate()</code> call.</p> <p>Subclasses implement :meth:<code>_compute_row_count</code>, :meth:<code>_compute_null_count</code>, and :meth:<code>_compute_unique_count</code>.</p>"},{"location":"api/column/","title":"Column and CrossColumnRule","text":""},{"location":"api/column/#column","title":"Column","text":"<p><code>Column</code> describes a required column and its constraints.</p> <p>Declared but not enforced</p> <p><code>dtype</code>, <code>freshness_minutes</code>, and <code>custom</code> are stored and serialized, but the current engines do not validate them. Use the enforced <code>pattern</code> constraint or a callable <code>CrossColumnRule</code> where appropriate.</p>"},{"location":"api/column/#dqflow.column.Column","title":"<code>dqflow.column.Column</code>  <code>dataclass</code>","text":"<p>Declare constraints and metadata for one required column.</p> <p>The current pandas and Polars engines enforce <code>not_null</code>, <code>min</code>, <code>max</code>, <code>allowed</code>, <code>unique</code>, and <code>pattern</code>. They preserve but do not yet enforce <code>dtype</code>, <code>freshness_minutes</code>, or <code>custom</code>.</p> <p>Attributes:</p> Name Type Description <code>dtype</code> <code>type | str</code> <p>Declared Python type or string type name. Currently descriptive.</p> <code>not_null</code> <code>bool</code> <p>Fail when the column contains null values.</p> <code>min</code> <code>Any | None</code> <p>Inclusive minimum value.</p> <code>max</code> <code>Any | None</code> <p>Inclusive maximum value.</p> <code>allowed</code> <code>Sequence[Any] | None</code> <p>Sequence of permitted non-null values.</p> <code>freshness_minutes</code> <code>int | None</code> <p>Declared maximum timestamp age. Not yet enforced.</p> <code>unique</code> <code>bool</code> <p>Require non-null values to be distinct.</p> <code>pattern</code> <code>str | None</code> <p>Regular expression applied to every non-null string value.</p> <code>description</code> <code>str</code> <p>Human-readable description.</p> <code>metadata</code> <code>dict[str, Any]</code> <p>User-defined metadata preserved on the contract.</p> <code>custom</code> <code>Callable[[Any], bool] | None</code> <p>Declared custom column callable. Not yet invoked by engines.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>min</code> is greater than <code>max</code>.</p>"},{"location":"api/column/#crosscolumnrule","title":"CrossColumnRule","text":"<p><code>CrossColumnRule</code> performs a row-wise comparison between columns, between a column and a literal, or through a Python callable.</p> <p>Structured rules support <code>&gt;=</code>, <code>&lt;=</code>, <code>&gt;</code>, <code>&lt;</code>, <code>==</code>, and <code>!=</code>. Callable rules are available only in Python contracts; they cannot be represented in YAML.</p> <p>See Cross-column and custom checks for examples.</p>"},{"location":"api/column/#dqflow.column.CrossColumnRule","title":"<code>dqflow.column.CrossColumnRule</code>  <code>dataclass</code>","text":"<p>Validate a row-wise relationship between columns or values.</p> <p>Supply either <code>check</code> or all three structured fields <code>left</code>, <code>op</code>, and <code>right</code>. A callable receives the complete DataFrame and must return a boolean mask with one value per row. Structured rules can be serialized to YAML; callable rules are Python-only.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Stable name used in the resulting check identifier.</p> <code>error_message</code> <code>str</code> <p>Message returned when at least one row fails.</p> <code>check</code> <code>Callable[[Any], Any] | None</code> <p>Callable accepting a pandas or Polars DataFrame and returning a boolean mask.</p> <code>left</code> <code>str | None</code> <p>Name of the left-hand column for a structured rule.</p> <code>op</code> <code>str | None</code> <p>Comparison operator: <code>&gt;=</code>, <code>&lt;=</code>, <code>&gt;</code>, <code>&lt;</code>, <code>==</code>, or <code>!=</code>.</p> <code>right</code> <code>str | int | float | None</code> <p>Right-hand column name or literal value.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the callable and structured forms are mixed, required structured fields are missing, or the operator is unsupported.</p>"},{"location":"api/context/","title":"ExecutionContext","text":"<p><code>ExecutionContext</code> bundles the runtime configuration for a single validation run: which engine to use, whether table-rule statistics are cached, and execution-mode flags. It is the third axis alongside the <code>Contract</code> (what the data must look like) and its compiled <code>ValidationSpec</code> (how it is checked) \u2014 the context is how the run executes.</p> <p><code>Contract.validate()</code> builds one from its <code>engine=</code> shortcut, or you can pass a context directly:</p> <pre><code>from dqflow import Contract, ExecutionContext\n\ncontext = ExecutionContext(engine=\"polars\", cache=True)\nresult = contract.validate(polars_df, context=context)\n</code></pre> <p>Only <code>engine</code> and <code>cache</code> change behaviour today. <code>parallel</code>, <code>max_workers</code>, <code>strict</code>, and <code>fail_fast</code> are carried but not yet acted on \u2014 they are the stable home for settings that #22 (parallel execution) and #44 (severity / thresholds) will consume.</p>"},{"location":"api/context/#dqflow.execution.context.ExecutionContext","title":"<code>dqflow.execution.context.ExecutionContext</code>  <code>dataclass</code>","text":"<p>Runtime configuration for one :meth:<code>~dqflow.contract.Contract.validate</code> call.</p> <p>Attributes:</p> Name Type Description <code>engine</code> <code>str</code> <p>Registered engine name to run the contract with (<code>\"pandas\"</code> / <code>\"polars\"</code>). Resolved through the engine registry by :meth:<code>resolve_engine</code>.</p> <code>parallel</code> <code>bool</code> <p>Reserved. Accepted and carried but not yet acted on \u2014 execution is always sequential today (#22).</p> <code>max_workers</code> <code>int | None</code> <p>Reserved companion to <code>parallel</code>; no effect yet.</p> <code>cache</code> <code>bool</code> <p>When true (default), table-rule statistics are memoised for the run so a column referenced by several rules is scanned once. Set to false to recompute every statistic on access.</p> <code>strict</code> <code>bool</code> <p>Reserved execution-mode flag; no effect yet (#44).</p> <code>fail_fast</code> <code>bool</code> <p>Reserved execution-mode flag; no effect yet. Unrelated to the <code>dq validate --fail-fast</code> option, which only controls the process exit code.</p>"},{"location":"api/context/#dqflow.execution.context.ExecutionContext.resolve_engine","title":"<code>resolve_engine()</code>","text":"<p>Return a fresh engine instance for :attr:<code>engine</code> via the registry.</p> <p>Raises:</p> Type Description <code>UnknownEngineError</code> <p>If :attr:<code>engine</code> is not registered.</p> <code>ImportError</code> <p>If the engine's optional dependency is missing.</p>"},{"location":"api/contract/","title":"Contract","text":"<p><code>Contract</code> is the top-level data contract. It groups column constraints, table rules, cross-column rules, descriptions, and metadata, then delegates validation to an engine.</p>"},{"location":"api/contract/#dqflow.contract.Contract","title":"<code>dqflow.contract.Contract</code>  <code>dataclass</code>","text":"<p>Define and execute data-quality expectations for a dataset.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Contract name included in validation results.</p> <code>columns</code> <code>dict[str, Column]</code> <p>Mapping of required column names to column definitions. Plain types and dictionaries are normalized to :class:<code>Column</code> objects.</p> <code>rules</code> <code>list[str]</code> <p>Table-rule expressions using <code>row_count</code>, <code>null_rate()</code>, and <code>unique_count()</code>.</p> <code>cross_column_rules</code> <code>list[CrossColumnRule]</code> <p>Structured or callable row-wise rules.</p> <code>description</code> <code>str</code> <p>Human-readable contract description.</p> <code>metadata</code> <code>dict[str, Any]</code> <p>User-defined metadata stored with the Python contract.</p>"},{"location":"api/contract/#dqflow.contract.Contract.validate","title":"<code>validate(df, engine=None, *, context=None)</code>","text":"<p>Validate a DataFrame and return every generated check.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>Any</code> <p>DataFrame supported by the selected engine.</p> required <code>engine</code> <code>Engine | str | None</code> <p>An :class:<code>~dqflow.engines.base.Engine</code> instance, a registered engine name (<code>\"pandas\"</code> or <code>\"polars\"</code>), or <code>None</code> to use the default pandas engine. A shortcut for the common case; mutually exclusive with <code>context</code>.</p> <code>None</code> <code>context</code> <code>ExecutionContext | None</code> <p>An :class:<code>~dqflow.execution.ExecutionContext</code> carrying the full runtime configuration (engine, cache, execution flags). When given, <code>engine</code> must be omitted. When an <code>Engine</code> instance is passed as <code>engine</code> a default context is used and its <code>engine</code> field is ignored.</p> <code>None</code> <p>Returns:</p> Type Description <code>ValidationResult</code> <p>A structured result whose <code>ok</code> property is true only when every</p> <code>ValidationResult</code> <p>check passes.</p>"},{"location":"api/contract/#dqflow.contract.Contract.from_yaml","title":"<code>from_yaml(path)</code>  <code>classmethod</code>","text":"<p>Load a contract from a YAML file.</p> <p>The file is validated against the contract schema before construction.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Path to the YAML contract.</p> required <p>Returns:</p> Type Description <code>Contract</code> <p>The parsed and normalized contract.</p> <p>Raises:</p> Type Description <code>ContractParseError</code> <p>If the file is not valid YAML.</p> <code>ContractSchemaError</code> <p>If the contract is structurally invalid. The exception carries path-aware :class:<code>~dqflow.schema.Diagnostic</code> records; <code>dq lint</code> prints them.</p> <code>ContractVersionError</code> <p>If <code>schema_version</code> is not supported.</p>"},{"location":"api/contract/#dqflow.contract.Contract.to_yaml","title":"<code>to_yaml(path, *, header=None)</code>","text":"<p>Write the serializable portion of this contract as YAML.</p> <p>The document declares the current <code>schema_version</code>. Callable cross-column rules and arbitrary metadata on <code>Column</code> are not serialized. Each line in <code>header</code> is written as a YAML comment.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>Destination path.</p> required <code>header</code> <code>str | None</code> <p>Optional provenance text to place above the YAML document.</p> <code>None</code>"},{"location":"api/contract/#notes","title":"Notes","text":"<ul> <li>Every declared column is required; a missing column produces a failed   <code>column_exists:&lt;name&gt;</code> check.</li> <li>The default engine is pandas. Pass <code>engine=PolarsEngine()</code> explicitly for a   Polars DataFrame.</li> <li>Callable cross-column rules cannot be serialized to YAML and are omitted by   <code>to_yaml()</code>.</li> </ul> <p>See Defining Contracts for an end-to-end example.</p>"},{"location":"api/diff/","title":"Contract diff","text":"<p><code>diff_contracts</code> compares two contracts and classifies each difference as breaking or non-breaking for data producers. The <code>dq diff</code> CLI command wraps it. See Diffing contracts for the classification table and the JSON schema.</p>"},{"location":"api/diff/#dqflow.diff.diff_contracts","title":"<code>dqflow.diff.diff_contracts(old, new)</code>","text":"<p>Compare two contracts and classify every difference.</p> <p>Parameters:</p> Name Type Description Default <code>old</code> <code>Contract | str | Path</code> <p>The previous contract, or a path/str to a YAML contract loaded with :meth:<code>Contract.from_yaml</code>.</p> required <code>new</code> <code>Contract | str | Path</code> <p>The updated contract, or a path/str to a YAML contract.</p> required <p>Returns:</p> Name Type Description <code>A</code> <code>ContractDiff</code> <p>class:<code>ContractDiff</code> whose <code>changes</code> are each classified</p> <code>ContractDiff</code> <p><code>\"breaking\"</code> or <code>\"non_breaking\"</code> for data producers.</p>"},{"location":"api/diff/#dqflow.diff.ContractDiff","title":"<code>dqflow.diff.ContractDiff</code>  <code>dataclass</code>","text":"<p>The full set of classified changes between two contracts.</p> <p>Attributes:</p> Name Type Description <code>old_name</code> <code>str</code> <p>Name of the previous contract.</p> <code>new_name</code> <code>str</code> <p>Name of the updated contract.</p> <code>changes</code> <code>list[ContractChange]</code> <p>Every difference, in a stable source order (columns in new- contract order, then table rules, then cross-column rules).</p>"},{"location":"api/diff/#dqflow.diff.ContractDiff.breaking_changes","title":"<code>breaking_changes</code>  <code>property</code>","text":"<p>Breaking changes, in source order.</p>"},{"location":"api/diff/#dqflow.diff.ContractDiff.non_breaking_changes","title":"<code>non_breaking_changes</code>  <code>property</code>","text":"<p>Non-breaking changes, in source order.</p>"},{"location":"api/diff/#dqflow.diff.ContractDiff.has_breaking","title":"<code>has_breaking</code>  <code>property</code>","text":"<p>Whether any change is breaking.</p>"},{"location":"api/diff/#dqflow.diff.ContractDiff.is_empty","title":"<code>is_empty</code>  <code>property</code>","text":"<p>Whether the two contracts are equivalent (no changes).</p>"},{"location":"api/diff/#dqflow.diff.ContractDiff.to_dict","title":"<code>to_dict()</code>","text":"<p>Return the stable, documented JSON representation of this diff.</p>"},{"location":"api/diff/#dqflow.diff.ContractDiff.render_text","title":"<code>render_text()</code>","text":"<p>Return the human-readable, severity-grouped summary.</p>"},{"location":"api/diff/#dqflow.diff.ContractChange","title":"<code>dqflow.diff.ContractChange</code>  <code>dataclass</code>","text":"<p>One classified difference between two contracts.</p> <p>Attributes:</p> Name Type Description <code>kind</code> <code>str</code> <p>Stable machine token \u2014 one of <code>column_added</code>, <code>column_removed</code>, <code>field_added</code>, <code>field_removed</code>, <code>field_changed</code>, <code>table_rule_added</code>, <code>table_rule_removed</code>, <code>cross_column_rule_added</code>, <code>cross_column_rule_removed</code>, <code>cross_column_rule_changed</code>.</p> <code>classification</code> <code>str</code> <p><code>\"breaking\"</code> or <code>\"non_breaking\"</code> for data producers.</p> <code>reason</code> <code>str</code> <p>Short human-readable explanation.</p> <code>column</code> <code>str | None</code> <p>Affected column name, or <code>None</code> for table-level and cross-column changes.</p> <code>attribute</code> <code>str | None</code> <p>Affected field name for <code>field_*</code> kinds (<code>dtype</code>/<code>not_null</code>/<code>min</code>/\u2026), otherwise <code>None</code>.</p> <code>old</code> <code>Any</code> <p>Previous value (<code>None</code> means the field or object was absent).</p> <code>new</code> <code>Any</code> <p>New value (<code>None</code> means the field or object was removed).</p>"},{"location":"api/diff/#dqflow.diff.ContractChange.breaking","title":"<code>breaking</code>  <code>property</code>","text":"<p>Whether this change is classified as breaking.</p>"},{"location":"api/diff/#dqflow.diff.ContractChange.to_dict","title":"<code>to_dict()</code>","text":"<p>Return a JSON-serializable representation of this change.</p>"},{"location":"api/engines/","title":"Engines","text":"<p>Engines execute the same contract against a DataFrame implementation and return the shared <code>ValidationResult</code> format.</p>"},{"location":"api/engines/#engine-interface","title":"Engine interface","text":"<p><code>Engine.validate()</code> takes the DataFrame, a <code>Contract</code> or prebuilt <code>ValidationSpec</code>, and an optional keyword-only <code>ExecutionContext</code> (<code>None</code> means engine defaults).</p>"},{"location":"api/engines/#dqflow.engines.base.Engine","title":"<code>dqflow.engines.base.Engine</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract interface implemented by DataFrame validation engines.</p>"},{"location":"api/engines/#dqflow.engines.base.Engine.validate","title":"<code>validate(data, contract, *, context=None)</code>  <code>abstractmethod</code>","text":"<p>Validate data against a contract.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Any</code> <p>Engine-specific DataFrame object.</p> required <code>contract</code> <code>Contract | ValidationSpec</code> <p>A :class:<code>~dqflow.contract.Contract</code> or an already-compiled :class:<code>~dqflow.spec.ValidationSpec</code>. A contract is compiled once on entry; engines execute the spec.</p> required <code>context</code> <code>ExecutionContext | None</code> <p>The run's :class:<code>~dqflow.execution.ExecutionContext</code>. <code>None</code> means engine defaults.</p> <code>None</code> <p>Returns:</p> Type Description <code>ValidationResult</code> <p>Engine-independent validation result.</p>"},{"location":"api/engines/#engine-registry","title":"Engine registry","text":"<p>Resolve an engine by name, or register your own. Importing this module does not import pandas or Polars.</p>"},{"location":"api/engines/#dqflow.engines.registry.get_engine","title":"<code>dqflow.engines.registry.get_engine(name=None)</code>","text":"<p>Return a new engine instance for <code>name</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str | None</code> <p>Registered engine name. <code>None</code> selects :data:<code>DEFAULT_ENGINE</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Engine</code> <p>A fresh :class:<code>~dqflow.engines.base.Engine</code>.</p> <p>Raises:</p> Type Description <code>UnknownEngineError</code> <p>If <code>name</code> is not registered.</p> <code>ImportError</code> <p>If the engine's optional dependency is missing.</p>"},{"location":"api/engines/#dqflow.engines.registry.register_engine","title":"<code>dqflow.engines.registry.register_engine(name, factory)</code>","text":"<p>Register <code>factory</code> under <code>name</code> for later :func:<code>get_engine</code> lookups.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Lower-case identifier, e.g. <code>\"spark\"</code>.</p> required <code>factory</code> <code>EngineFactory</code> <p>Zero-argument callable returning a fresh engine instance.</p> required"},{"location":"api/engines/#dqflow.engines.registry.available_engines","title":"<code>dqflow.engines.registry.available_engines()</code>","text":"<p>Return the registered engine names in sorted order.</p>"},{"location":"api/engines/#pandasengine","title":"PandasEngine","text":""},{"location":"api/engines/#dqflow.engines.pandas.PandasEngine","title":"<code>dqflow.engines.pandas.PandasEngine</code>","text":"<p>               Bases: <code>Engine</code></p> <p>Execute contracts against in-memory pandas DataFrames.</p>"},{"location":"api/engines/#polarsengine","title":"PolarsEngine","text":"<p>The Polars engine is experimental and requires <code>pip install \"dqflow[polars]\"</code>. It accepts both <code>polars.DataFrame</code> and <code>polars.LazyFrame</code>; a lazy frame is currently collected before validation.</p> <p>See Choosing an engine for usage and trade-offs.</p>"},{"location":"api/engines/#dqflow.engines.polars.PolarsEngine","title":"<code>dqflow.engines.polars.PolarsEngine</code>","text":"<p>               Bases: <code>Engine</code></p> <p>Execute contracts against Polars DataFrames or LazyFrames.</p> <p>LazyFrames are currently collected before checks run, so validation is not streaming or lazy yet.</p>"},{"location":"api/inference/","title":"Contract inference","text":"<p><code>infer_contract</code> creates a draft contract from observed pandas data. Inference is a starting point: review sampled ranges, allowed values, uniqueness, and patterns before committing the contract.</p> <p>The CLI adds a provenance header with this helper:</p>"},{"location":"api/inference/#dqflow.inference.infer_contract","title":"<code>dqflow.inference.infer_contract(df, name='inferred', *, infer_ranges=True, max_allowed_cardinality=20)</code>","text":"<p>Infer a draft contract from the observed values in <code>df</code>.</p> <p>The result describes only the supplied sample. Callers should review the generated constraints before treating them as a durable specification.</p> <p>Parameters:</p> Name Type Description Default <code>df</code> <code>DataFrame</code> <p>Source pandas DataFrame or representative sample.</p> required <code>name</code> <code>str</code> <p>Name for the inferred contract.</p> <code>'inferred'</code> <code>infer_ranges</code> <code>bool</code> <p>Whether to infer observed numeric and datetime bounds.</p> <code>True</code> <code>max_allowed_cardinality</code> <code>int</code> <p>Maximum distinct string/category values that can become an <code>allowed</code> constraint. Set to zero to disable it.</p> <code>20</code> <p>Returns:</p> Type Description <code>Contract</code> <p>A draft contract preserving source column order.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If <code>max_allowed_cardinality</code> is negative.</p>"},{"location":"api/inference/#dqflow.inference.inference_header","title":"<code>dqflow.inference.inference_header(source, row_count, *, inferred_at=None)</code>","text":"<p>Build the provenance comment placed above an inferred YAML contract.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>str</code> <p>Human-readable source file name.</p> required <code>row_count</code> <code>int</code> <p>Number of observed rows.</p> required <code>inferred_at</code> <code>datetime | None</code> <p>Optional timestamp, primarily for deterministic output.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Two lines of provenance and review guidance.</p>"},{"location":"api/result/","title":"Validation results","text":""},{"location":"api/result/#validationresult","title":"ValidationResult","text":"<p><code>ValidationResult</code> contains every check produced by an engine.</p>"},{"location":"api/result/#dqflow.result.ValidationResult","title":"<code>dqflow.result.ValidationResult</code>  <code>dataclass</code>","text":"<p>Aggregate all checks produced while validating one contract.</p> <p>Attributes:</p> Name Type Description <code>contract_name</code> <code>str</code> <p>Name of the validated contract.</p> <code>checks</code> <code>list[CheckResult]</code> <p>Checks in engine execution order.</p>"},{"location":"api/result/#dqflow.result.ValidationResult.ok","title":"<code>ok</code>  <code>property</code>","text":"<p>Return whether every check passed.</p>"},{"location":"api/result/#dqflow.result.ValidationResult.failed_checks","title":"<code>failed_checks</code>  <code>property</code>","text":"<p>Return failed checks in execution order.</p>"},{"location":"api/result/#dqflow.result.ValidationResult.summary","title":"<code>summary()</code>","text":"<p>Return a compact text summary including every failed check.</p>"},{"location":"api/result/#dqflow.result.ValidationResult.to_dict","title":"<code>to_dict()</code>","text":"<p>Return a JSON-serializable representation of this result.</p>"},{"location":"api/result/#checkresult","title":"CheckResult","text":"<p><code>CheckResult</code> represents one schema, column, table, or cross-column check.</p> <p><code>ValidationResult.to_dict()</code> recursively converts NumPy scalars and sets into JSON-serializable Python values, making it suitable for logs and CI artifacts.</p>"},{"location":"api/result/#dqflow.result.CheckResult","title":"<code>dqflow.result.CheckResult</code>  <code>dataclass</code>","text":"<p>Represent one schema, column, table, or cross-column check.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Stable check identifier such as <code>not_null:order_id</code>.</p> <code>passed</code> <code>bool</code> <p>Whether the check passed.</p> <code>message</code> <code>str</code> <p>Human-readable failure detail; empty for passing checks.</p> <code>details</code> <code>dict[str, Any]</code> <p>Structured values useful for logs and reports.</p>"},{"location":"api/result/#reporting-details","title":"Reporting details","text":"<p>Failing checks attach extra keys to <code>CheckResult.details</code> for reporting. The set depends on the check kind:</p> Check Keys <code>not_null</code> <code>null_count</code>, <code>null_rate</code> <code>min</code> / <code>max</code> <code>actual_min</code> / <code>actual_max</code>, <code>violating_rows</code>, <code>violating_rate</code> <code>allowed</code> <code>invalid_values</code>, <code>sample_invalid_values</code>, <code>invalid_value_count</code>, <code>violating_rows</code>, <code>violating_rate</code> <code>unique</code> <code>duplicate_count</code>, <code>sample_duplicate_values</code>, <code>violating_rate</code> <code>pattern</code> <code>invalid_count</code>, <code>sample_invalid_values</code>, <code>violating_rate</code> cross-column <code>failing_rows</code>, <code>failing_rate</code> <p>Value samples are bounded (at most five, sorted). <code>dqflow.report.render_result()</code> turns a <code>ValidationResult</code> into the grouped text that <code>dq validate</code> prints; see CLI usage.</p>"},{"location":"api/rules/","title":"Table-rule evaluator","text":"<p><code>dqflow.rules.evaluate_rule</code> is the single evaluator for table-rule expressions, shared by every engine. It parses the expression with <code>ast</code> and walks a strict whitelist \u2014 <code>row_count</code>, <code>null_rate('col')</code>, <code>unique_count('col')</code>, literals, <code>and</code> / <code>or</code> / <code>not</code>, arithmetic, and comparisons. No <code>eval</code>, no attribute access, no builtins.</p> <p>See Table rules for the expression language and the safety model.</p>"},{"location":"api/rules/#dqflow.rules.evaluate_rule","title":"<code>dqflow.rules.evaluate_rule(expression, *, row_count, null_rate, unique_count)</code>","text":"<p>Evaluate <code>expression</code> and return its truthiness as a <code>bool</code>.</p> <p>Parameters:</p> Name Type Description Default <code>expression</code> <code>str</code> <p>The rule text, e.g. <code>\"row_count &gt; 0 and null_rate('x') &lt; 0.1\"</code>.</p> required <code>row_count</code> <code>int</code> <p>Value bound to the <code>row_count</code> name.</p> required <code>null_rate</code> <code>Callable[[str], float]</code> <p>Called with a column name for <code>null_rate('col')</code>.</p> required <code>unique_count</code> <code>Callable[[str], float]</code> <p>Called with a column name for <code>unique_count('col')</code>.</p> required <p>Raises:</p> Type Description <code>RuleError</code> <p>If the expression is syntactically invalid or uses a construct outside the supported whitelist.</p>"},{"location":"api/rules/#dqflow.rules.RuleError","title":"<code>dqflow.rules.RuleError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a table-rule expression cannot be parsed or is not allowed.</p>"},{"location":"api/schema/","title":"Contract schema","text":"<p><code>dqflow.schema</code> validates a contract document \u2014 the YAML / serialisable form \u2014 and reports precise, path-aware diagnostics instead of raw tracebacks. <code>Contract.from_yaml()</code> runs this validation before it constructs anything; <code>dq lint</code> exposes it directly.</p> <p>See Linting contracts for the diagnostic-code table and Schema versioning for the compatibility policy.</p>"},{"location":"api/schema/#validation","title":"Validation","text":""},{"location":"api/schema/#dqflow.schema.lint_contract_file","title":"<code>dqflow.schema.lint_contract_file(path)</code>","text":"<p>Read, parse and validate the contract at <code>path</code>.</p> <p>Raises:</p> Type Description <code>ContractParseError</code> <p>If the file is not valid YAML.</p> <p>Returns:</p> Name Type Description <code>Every</code> <code>list[Diagnostic]</code> <p>class:<code>Diagnostic</code> found, errors and warnings, in document order.</p>"},{"location":"api/schema/#dqflow.schema.lint_contract_data","title":"<code>dqflow.schema.lint_contract_data(data)</code>","text":"<p>Validate an already-parsed contract mapping. Pure; performs no I/O.</p>"},{"location":"api/schema/#dqflow.schema.Diagnostic","title":"<code>dqflow.schema.Diagnostic</code>  <code>dataclass</code>","text":"<p>One problem found in a contract document.</p> <p>Attributes:</p> Name Type Description <code>severity</code> <code>str</code> <p><code>\"error\"</code> (the contract cannot be loaded) or <code>\"warning\"</code> (loadable, but probably not intended).</p> <code>code</code> <code>str</code> <p>Stable kebab-case token, e.g. <code>\"unknown-field\"</code>. Safe to branch on; the human <code>message</code> is not.</p> <code>message</code> <code>str</code> <p>Human-readable, actionable description.</p> <code>path</code> <code>str</code> <p>Location in the document \u2014 <code>\"columns.amount.min\"</code>, <code>\"rules[2]\"</code>, or <code>\"\"</code> for the document root.</p> <code>line</code> <code>int | None</code> <p>1-based line in the source file when known, else <code>None</code>.</p>"},{"location":"api/schema/#dqflow.schema.Diagnostic.is_error","title":"<code>is_error</code>  <code>property</code>","text":"<p>Whether this diagnostic is error severity.</p>"},{"location":"api/schema/#dqflow.schema.Diagnostic.to_dict","title":"<code>to_dict()</code>","text":"<p>Return a JSON-serialisable representation.</p>"},{"location":"api/schema/#exceptions","title":"Exceptions","text":"<p>Raised by <code>Contract.from_yaml()</code>; all derive from <code>ContractError</code>.</p>"},{"location":"api/schema/#dqflow.schema.ContractError","title":"<code>dqflow.schema.ContractError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base class for every contract-loading failure.</p>"},{"location":"api/schema/#dqflow.schema.ContractParseError","title":"<code>dqflow.schema.ContractParseError</code>","text":"<p>               Bases: <code>ContractError</code></p> <p>The contract file could not be parsed as YAML.</p>"},{"location":"api/schema/#dqflow.schema.ContractSchemaError","title":"<code>dqflow.schema.ContractSchemaError</code>","text":"<p>               Bases: <code>ContractError</code></p> <p>The contract parsed but is structurally invalid.</p> <p>Attributes:</p> Name Type Description <code>diagnostics</code> <p>The error-severity :class:<code>Diagnostic</code> records that caused the failure (warnings are not included here).</p> <code>source</code> <p>The contract file path, when loaded from one.</p>"},{"location":"api/schema/#dqflow.schema.ContractVersionError","title":"<code>dqflow.schema.ContractVersionError</code>","text":"<p>               Bases: <code>ContractError</code></p> <p>The contract's <code>schema_version</code> is not supported by this dqflow release.</p>"},{"location":"api/schema/#version","title":"Version","text":""},{"location":"api/schema/#dqflow.schema.SCHEMA_VERSION","title":"<code>dqflow.schema.SCHEMA_VERSION = '1.0'</code>  <code>module-attribute</code>","text":""},{"location":"api/schema/#dqflow.schema.SUPPORTED_SCHEMA_VERSIONS","title":"<code>dqflow.schema.SUPPORTED_SCHEMA_VERSIONS = ('1.0',)</code>  <code>module-attribute</code>","text":""},{"location":"api/schema/#published-json-schema","title":"Published JSON Schema","text":"<p>A JSON Schema for the serialisable contract format is published for editor tooling and shipped inside the package. It is a subset of the linter \u2014 it cannot express cross-field checks.</p>"},{"location":"api/schema/#dqflow.schema.contract_json_schema","title":"<code>dqflow.schema.contract_json_schema()</code>","text":"<p>Return the published JSON Schema for the current contract format as a dict.</p>"},{"location":"api/schema/#dqflow.schema.CONTRACT_SCHEMA_URI","title":"<code>dqflow.schema.CONTRACT_SCHEMA_URI = f'https://dqflow.github.io/dqflow/schema/{CONTRACT_SCHEMA_FILENAME}'</code>  <code>module-attribute</code>","text":""},{"location":"api/spec/","title":"ValidationSpec","text":"<p><code>ValidationSpec</code> is the engine-agnostic intermediate representation of a contract. <code>Contract.validate()</code> compiles the contract with <code>ValidationSpec.from_contract()</code> and hands the spec to an engine; engines execute the flat list of <code>CheckSpec</code> entries and never read <code>Column</code> fields directly.</p> <p>Most users never construct these types \u2014 they are the contract between <code>Contract</code> and the engines, and the extension point for new engines.</p>"},{"location":"api/spec/#dqflow.spec.ValidationSpec","title":"<code>dqflow.spec.ValidationSpec</code>  <code>dataclass</code>","text":"<p>A contract compiled into an ordered, engine-agnostic list of checks.</p> <p>Attributes:</p> Name Type Description <code>contract_name</code> <code>str</code> <p>Name carried onto the :class:<code>~dqflow.result.ValidationResult</code>.</p> <code>checks</code> <code>tuple[CheckSpec, ...]</code> <p>Checks in canonical execution order \u2014 every <code>column_exists</code> first, then per-column constraints (in <code>Column</code> field order), then table rules, then cross-column rules.</p>"},{"location":"api/spec/#dqflow.spec.ValidationSpec.from_contract","title":"<code>from_contract(contract)</code>  <code>classmethod</code>","text":"<p>Compile <code>contract</code> into a :class:<code>ValidationSpec</code>.</p>"},{"location":"api/spec/#dqflow.spec.CheckSpec","title":"<code>dqflow.spec.CheckSpec</code>  <code>dataclass</code>","text":"<p>One atomic check for an engine to execute.</p> <p>Attributes:</p> Name Type Description <code>kind</code> <code>str</code> <p>One of :data:<code>CHECK_KINDS</code>.</p> <code>target</code> <code>str</code> <p>Column name, table-rule expression, or cross-column rule name.</p> <code>name</code> <code>str</code> <p>The stable :class:<code>~dqflow.result.CheckResult</code> identifier, e.g. <code>\"min:amount\"</code> \u2014 precomputed so engines never build identifiers.</p> <code>params</code> <code>Mapping[str, Any]</code> <p>Kind-specific parameters (<code>{\"min\": 0}</code>, <code>{\"pattern\": \"...\"}</code>, <code>{\"expression\": \"row_count &gt; 0\"}</code>, <code>{\"rule\": CrossColumnRule(...)}</code>).</p>"},{"location":"api/spec/#dqflow.spec.CHECK_KINDS","title":"<code>dqflow.spec.CHECK_KINDS = frozenset({'column_exists', 'not_null', 'min', 'max', 'allowed', 'unique', 'pattern', 'rule', 'cross_column'})</code>  <code>module-attribute</code>","text":""},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.9 or higher</li> <li>pandas 1.5.0 or higher</li> </ul>"},{"location":"getting-started/installation/#install-from-pypi","title":"Install from PyPI","text":"<pre><code>pip install dqflow\n</code></pre> <p>Optional features are installed explicitly:</p> <pre><code># Experimental Polars engine\npip install \"dqflow[polars]\"\n\n# Parquet input for dq validate / dq infer\npip install \"dqflow[parquet]\"\n</code></pre> <p>CSV and JSON input do not require a file-format extra. Parquet uses <code>pyarrow</code> from the <code>parquet</code> extra.</p>"},{"location":"getting-started/installation/#install-from-source","title":"Install from source","text":"<pre><code>git clone https://github.com/dqflow/dqflow.git\ncd dqflow\npip install -e .\n</code></pre>"},{"location":"getting-started/installation/#development-installation","title":"Development installation","text":"<p>For contributing to dqflow:</p> <pre><code>git clone https://github.com/dqflow/dqflow.git\ncd dqflow\npip install -e \".[dev,docs,polars]\"\npre-commit install\n</code></pre>"},{"location":"getting-started/installation/#verify-installation","title":"Verify installation","text":"<pre><code>dq --version\n</code></pre> <pre><code>import dqflow\nprint(dqflow.__version__)\n</code></pre>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":"<p>This guide will help you validate your first DataFrame in under 5 minutes.</p> <p>Note</p> <p><code>dtype</code>, <code>freshness_minutes</code>, and <code>custom</code> are declarative fields that are not enforced yet. Column existence, <code>not_null</code>, <code>min</code>, <code>max</code>, <code>allowed</code>, <code>unique</code>, and regex <code>pattern</code> constraints are enforced.</p>"},{"location":"getting-started/quickstart/#basic-example","title":"Basic Example","text":"<pre><code>import pandas as pd\nfrom dqflow import Contract, Column\n\n# Sample data with quality issues\ndf = pd.DataFrame({\n    \"order_id\": [\"A001\", None, \"A003\"],      # Has null value\n    \"amount\": [100.0, -50.0, 75.0],          # Has negative value\n    \"currency\": [\"USD\", \"GBP\", \"EUR\"],       # GBP not in allowed list\n})\n\n# Define your data contract\ncontract = Contract(\n    name=\"orders\",\n    columns={\n        \"order_id\": Column(str, not_null=True),\n        \"amount\": Column(float, min=0),\n        \"currency\": Column(str, allowed=[\"USD\", \"EUR\"]),\n    },\n    rules=[\"row_count &gt; 0\"],\n)\n\n# Validate\nresult = contract.validate(df)\n\n# Check results\nprint(result.summary())\n</code></pre> <p>Output:</p> <pre><code>Contract 'orders': 4/7 checks passed\nFailed checks:\n  - not_null:order_id: Column 'order_id' has 1 null value\n  - min:amount: Column 'amount' has 1 value below the minimum 0\n  - allowed:currency: Column 'currency' has 1 value outside the allowed set\n</code></pre>"},{"location":"getting-started/quickstart/#use-in-pipelines","title":"Use in Pipelines","text":"<pre><code>if not result.ok:\n    raise Exception(result.summary())  # Fail fast!\n</code></pre>"},{"location":"getting-started/quickstart/#json-output","title":"JSON Output","text":"<p>For logging and monitoring:</p> <pre><code>import json\nprint(json.dumps(result.to_dict(), indent=2))\n</code></pre>"},{"location":"getting-started/quickstart/#next-steps","title":"Next Steps","text":"<ul> <li>Column Validations - Learn all column checks</li> <li>Table Rules - Add table-level validations</li> <li>YAML Contracts - Define contracts in YAML</li> <li>CLI Usage - Use the command-line interface</li> </ul>"},{"location":"guide/cli/","title":"CLI usage","text":"<p>The <code>dq</code> command validates files, inspects contracts, and infers draft contracts.</p>"},{"location":"guide/cli/#install-file-format-support","title":"Install file-format support","text":"<p>CSV and JSON work with the base installation. Parquet requires a pandas Parquet backend:</p> <pre><code>pip install dqflow\npip install \"dqflow[parquet]\"  # adds pyarrow\n</code></pre>"},{"location":"guide/cli/#dq-validate","title":"<code>dq validate</code>","text":"<pre><code>dq validate CONTRACT DATA [--output text|json] [--engine pandas|polars]\n             [--fail-fast] [--quiet | --verbose] [--color | --no-color]\n</code></pre> <p>Supported extensions are <code>.csv</code>, <code>.json</code>, and\u2014after installing the extra\u2014 <code>.parquet</code>.</p> <pre><code>dq validate contracts/orders.yaml data/orders.csv\ndq validate contracts/orders.yaml data/orders.csv --output json\ndq validate contracts/orders.yaml data/orders.parquet --fail-fast\n</code></pre> <p><code>--engine</code> selects the validation engine (default <code>pandas</code>). <code>--engine polars</code> reads the data file with Polars' native readers and runs the Polars engine, which needs <code>pip install \"dqflow[polars]\"</code>. Both engines produce the same result shape.</p> <p>Without <code>--fail-fast</code>, a completed validation command exits <code>0</code> even when checks fail; inspect the text or JSON result. With <code>--fail-fast</code>, the command still evaluates and prints all checks, then exits <code>1</code> when the result is not OK. The option does not short-circuit at the first failure.</p>"},{"location":"guide/cli/#text-output","title":"Text output","text":"<p>The default renderer groups checks into schema, columns, table rules, and cross-column rules, shows per-group pass/fail counts, and appends a failure rate and a bounded sample of offending values to each failing check.</p> <pre><code>$ dq validate contracts/orders.yaml data/orders.csv\norders \u00b7 5 of 8 checks failed on 4 rows\n\n  Schema  3/3 passed\n\n  Columns  4/4 failed\n    order_id  \u2718 not_null  has 1 null value (25.0%)\n              \u2718 unique    has 2 non-unique values (50.0%)  \u00b7  e.g. 'A001'\n    amount    \u2718 min       has 1 value below the minimum 0 (25.0%)\n    currency  \u2718 allowed   has 1 value outside the allowed set (25.0%)  \u00b7  e.g. 'GBP'\n\n  Table rules  1/1 failed\n    \u2718 null_rate('order_id') == 0\n\n  3 passed \u00b7 5 failed\n</code></pre> <ul> <li><code>-q</code> / <code>--quiet</code> prints only failing checks (plus the summary line).</li> <li><code>-v</code> / <code>--verbose</code> prints every check, passing ones included.</li> <li>Colour is used on a TTY and suppressed when the output is piped or when the   <code>NO_COLOR</code> environment variable is set. <code>--color</code> / <code>--no-color</code> overrides the   detection.</li> </ul> <p><code>--output json</code> is unaffected by these flags. Its schema is unchanged; failing checks now carry extra <code>details</code> keys (<code>null_rate</code>, <code>violating_rows</code>, <code>violating_rate</code>, <code>sample_invalid_values</code>, <code>sample_duplicate_values</code>, <code>failing_rate</code>).</p>"},{"location":"guide/cli/#dq-lint","title":"<code>dq lint</code>","text":"<pre><code>dq lint CONTRACT [--output text|json] [--strict]\n</code></pre> <p>Checks a contract file for schema and structural problems \u2014 unknown fields, wrong types, bad regexes, contradictory bounds, unparseable rules, an unsupported schema version \u2014 without reading any data. Every diagnostic carries a document path and, where possible, a line number.</p> <pre><code>dq lint contracts/orders.yaml\ndq lint contracts/orders.yaml --output json\ndq lint contracts/orders.yaml --strict   # exit 1 on warnings too\n</code></pre> <p>Exits <code>0</code> when there are no errors, <code>1</code> when there are (or, with <code>--strict</code>, when there are warnings). See Linting contracts for the full diagnostic list.</p>"},{"location":"guide/cli/#dq-schema","title":"<code>dq schema</code>","text":"<pre><code>dq schema\n</code></pre> <p>Prints the JSON Schema for the contract format. The same document is published at <code>https://dqflow.github.io/dqflow/schema/contract-1.0.json</code> for editor integration. It is a subset of <code>dq lint</code> \u2014 it cannot express cross-field checks \u2014 so keep <code>dq lint</code> in CI.</p>"},{"location":"guide/cli/#dq-diff","title":"<code>dq diff</code>","text":"<pre><code>dq diff OLD NEW [--output text|json] [--allow-breaking]\n</code></pre> <p>Compares two contract YAML files and classifies each difference as breaking or non-breaking for data producers. Exits <code>1</code> when a breaking change is present; <code>--allow-breaking</code> forces exit <code>0</code>.</p> <pre><code>dq diff contracts/orders@v1.yaml contracts/orders@v2.yaml\ndq diff old.yaml new.yaml --output json\n</code></pre> <p>See Diffing contracts for the full classification table and the JSON schema.</p>"},{"location":"guide/cli/#dq-show","title":"<code>dq show</code>","text":"<pre><code>dq show contracts/orders.yaml\n</code></pre> <p>This prints the contract description, declared columns and constraints, and table rules. Declared dtype and freshness values may appear even though the engines do not enforce them yet.</p>"},{"location":"guide/cli/#dq-infer","title":"<code>dq infer</code>","text":"<pre><code>dq infer DATA OUTPUT \\\n  --sample 100000 \\\n  --max-allowed-cardinality 20\n</code></pre> <p>Useful options:</p> <ul> <li><code>--sample N</code> reads at most <code>N</code> rows.</li> <li><code>--no-ranges</code> omits observed min/max bounds.</li> <li><code>--max-allowed-cardinality N</code> controls enum inference; zero disables it.</li> <li><code>--strict</code> rejects malformed CSV rows instead of skipping them.</li> </ul> <p>Inference creates a draft, not a production specification. See Infer and refine a contract.</p>"},{"location":"guide/cli/#ci","title":"CI","text":"<p>Use <code>--fail-fast</code> to turn a failed contract into a failed job:</p> <pre><code>- name: Validate orders\n  run: dq validate contracts/orders.yaml data/orders.csv --fail-fast\n</code></pre> <p>For a complete workflow, see Gate a pull request.</p>"},{"location":"guide/columns/","title":"Column validations","text":"<p>Every entry in <code>Contract.columns</code> is required. The engine first emits a <code>column_exists:&lt;name&gt;</code> check and then evaluates the configured constraints when the column exists.</p> <pre><code>from dqflow import Column\n\nColumn(str, not_null=True, unique=True, pattern=r\"^[A-Z]\\d{4}$\")\nColumn(float, min=0, max=100_000)\nColumn(str, allowed=[\"USD\", \"EUR\"])\n</code></pre>"},{"location":"guide/columns/#enforced-constraints","title":"Enforced constraints","text":"Constraint Behavior <code>not_null=True</code> Fails when any value is null/NaN <code>min=value</code> Requires the observed minimum to be at least <code>value</code> <code>max=value</code> Requires the observed maximum to be at most <code>value</code> <code>allowed=[...]</code> Rejects non-null values outside the sequence <code>unique=True</code> Rejects duplicated non-null values; nulls are ignored <code>pattern=...</code> Requires every non-null string to match the regex <p>Combine <code>unique=True</code> with <code>not_null=True</code> when null values must also fail.</p> <pre><code>from dqflow import Column, Contract\n\ncontract = Contract(\n    name=\"orders\",\n    columns={\n        \"order_id\": Column(str, not_null=True, unique=True, pattern=r\"^A\\d{3}$\"),\n        \"amount\": Column(float, min=0, max=100_000),\n        \"currency\": Column(str, allowed=[\"USD\", \"EUR\"]),\n    },\n)\n</code></pre>"},{"location":"guide/columns/#declared-but-not-enforced","title":"Declared but not enforced","text":"<p><code>dtype</code>, <code>freshness_minutes</code>, and <code>custom</code> are currently descriptive fields. They are retained on <code>Column</code>; dtype and freshness can be written to YAML and displayed by the CLI, but neither validation engine checks them yet.</p> <pre><code>Column(\"timestamp\", freshness_minutes=60)  # declaration only today\nColumn(str, custom=lambda value: bool(value))  # declaration only today\n</code></pre> <p>For custom logic that runs today, use a callable <code>CrossColumnRule</code>. Track future enforcement in #51.</p>"},{"location":"guide/columns/#metadata","title":"Metadata","text":"<p>Descriptions and metadata help document ownership or classification without changing validation behavior:</p> <pre><code>Column(\n    dtype=str,\n    not_null=True,\n    description=\"Unique customer identifier\",\n    metadata={\"pii\": False, \"owner\": \"checkout\"},\n)\n</code></pre>"},{"location":"guide/contracts/","title":"Defining contracts","text":"<p>A <code>Contract</code> defines required columns and quality checks for one dataset.</p> <pre><code>from dqflow import Column, Contract\n\ncontract = Contract(\n    name=\"orders\",\n    description=\"Orders emitted by checkout\",\n    columns={\n        \"order_id\": Column(str, not_null=True, unique=True),\n        \"amount\": Column(float, min=0),\n    },\n    rules=[\n        \"row_count &gt; 0\",\n        \"null_rate('amount') &lt; 0.01\",\n    ],\n    metadata={\"owner\": \"data-platform\"},\n)\n</code></pre> <p><code>dtype</code> is part of the declaration but is not checked yet. The current engines enforce column existence and the constraints listed in Column validations.</p>"},{"location":"guide/contracts/#validate-data","title":"Validate data","text":"<pre><code>import pandas as pd\n\ndf = pd.read_csv(\"orders.csv\")\nresult = contract.validate(df)\n\nif not result.ok:\n    raise ValueError(result.summary())\n</code></pre> <p>The returned <code>ValidationResult</code> provides:</p> <ul> <li><code>ok</code>: whether every check passed;</li> <li><code>checks</code>: all <code>CheckResult</code> objects;</li> <li><code>failed_checks</code>: only failed checks;</li> <li><code>summary()</code>: human-readable text;</li> <li><code>to_dict()</code>: JSON-serializable output.</li> </ul>"},{"location":"guide/contracts/#yaml-contracts","title":"YAML contracts","text":"<pre><code>from dqflow import Contract\n\ncontract = Contract.from_yaml(\"contracts/orders.yaml\")\ncontract.to_yaml(\"contracts/orders-copy.yaml\")\n</code></pre> <p>Use YAML for reviewable declarative contracts and Python when you need callable cross-column rules. See YAML contracts.</p>"},{"location":"guide/contracts/#select-an-engine","title":"Select an engine","text":"<p>Pandas is the default. Polars is explicit:</p> <pre><code>from dqflow.engines.polars import PolarsEngine\n\nresult = contract.validate(polars_df, engine=PolarsEngine())\n</code></pre> <p>See Choosing an engine for installation and current limitations.</p>"},{"location":"guide/custom-checks/","title":"Cross-column and custom checks","text":"<p><code>CrossColumnRule</code> is the supported way to run row-wise relationships. Use the structured form when possible because it works in both Python and YAML.</p>"},{"location":"guide/custom-checks/#compare-two-columns","title":"Compare two columns","text":"<pre><code>from dqflow import Contract, CrossColumnRule\n\ncontract = Contract(\n    name=\"orders\",\n    cross_column_rules=[\n        CrossColumnRule(\n            name=\"shipped_after_created\",\n            left=\"shipped_at\",\n            op=\"&gt;=\",\n            right=\"created_at\",\n            error_message=\"shipped_at must not precede created_at\",\n        )\n    ],\n)\n</code></pre> <p>The right side may also be a numeric or string literal. When a string matches an existing column name, it is treated as a column reference.</p>"},{"location":"guide/custom-checks/#add-a-callable-check","title":"Add a callable check","text":"<p>For logic that cannot be expressed as one comparison, pass a function that returns one boolean value per row:</p> <pre><code>import pandas as pd\nfrom dqflow import Contract, CrossColumnRule\n\n\ndef valid_discount(df: pd.DataFrame) -&gt; pd.Series:\n    return (df[\"discount\"] &gt;= 0) &amp; (df[\"discount\"] &lt;= df[\"subtotal\"])\n\n\ncontract = Contract(\n    name=\"orders\",\n    cross_column_rules=[\n        CrossColumnRule(\n            name=\"valid_discount\",\n            check=valid_discount,\n            error_message=\"discount must be between zero and subtotal\",\n        )\n    ],\n)\n</code></pre> <p>Callable rules are trusted Python code and are not serialized by <code>to_yaml()</code>. Keep them in version-controlled Python modules. The <code>Column.custom</code> field is only declarative today and is not invoked by either engine.</p>"},{"location":"guide/custom-checks/#failure-details","title":"Failure details","text":"<p>A cross-column result is named <code>cross_column:&lt;rule-name&gt;</code> and includes <code>details[\"failing_rows\"]</code>. Exceptions raised by the callable become a failed check with an evaluation-error message.</p>"},{"location":"guide/diff/","title":"Diffing contracts","text":"<p><code>dq diff</code> compares two contract versions and classifies every difference as breaking or non-breaking for data producers. Use it in code review and CI to catch a tightened constraint before it rejects data that used to pass.</p> <pre><code>dq diff OLD NEW [--output text|json] [--allow-breaking]\n</code></pre> <pre><code>$ dq diff contracts/orders@v1.yaml contracts/orders@v2.yaml\norders: 3 changes (1 breaking)\n\n  BREAKING\n    ~ column \"amount\" min: 0 -&gt; 10        (stricter lower bound)\n\n  non-breaking\n    ~ column \"currency\" allowed: +[JPY]  (widened allowed set)\n    + column \"discount\" (float)          (new nullable column)\n\n$ echo $?\n1\n</code></pre> <p><code>OLD</code> and <code>NEW</code> are contract YAML files. The command exits <code>1</code> when any breaking change is present so a CI job fails the pull request; pass <code>--allow-breaking</code> to force exit <code>0</code>.</p>"},{"location":"guide/diff/#what-counts-as-breaking","title":"What counts as breaking","text":"<p>A change is breaking when data that conformed to the old contract may violate the new one. Classification follows the contract's declared intent \u2014 <code>dtype</code> and <code>freshness_minutes</code> are compared even though the engines do not enforce them yet (roadmap).</p> Change Breaking Non-breaking column added new column is <code>not_null</code> or <code>unique</code> new column is nullable column removed \u2014 always (a dropped requirement only lets more data pass) <code>dtype</code> any change except widening (<code>float -&gt; integer</code>, <code>string -&gt; integer</code>, \u2026) <code>integer -&gt; float</code> <code>not_null</code> added (<code>false -&gt; true</code>) removed (<code>true -&gt; false</code>) <code>unique</code> added removed <code>min</code> added; raised removed; lowered <code>max</code> added; lowered removed; raised <code>allowed</code> added; any value removed from the set removed; only additions to the set <code>pattern</code> added; any change to an existing pattern removed <code>freshness_minutes</code> added; decreased removed; increased table rule added removed cross-column rule added; redefined (same <code>name</code>, different <code>left</code>/<code>op</code>/<code>right</code>) removed <p>Notes:</p> <ul> <li>dqflow requires every declared column to be present in the data. A nullable   new column is treated as non-breaking on the assumption that the producer can   emit it (even as all-null); a <code>not_null</code> new column demands real values and is   breaking. If your producers may omit columns entirely, treat any column   addition as breaking and run with <code>--allow-breaking</code> off.</li> <li>Table rules are opaque expressions, so <code>dq diff</code> matches them by exact string.   A reworded rule (e.g. <code>null_rate('a') &lt; 0.5</code> \u2192 <code>&lt; 0.2</code>) shows as one removal   and one addition rather than a single \"changed\" entry.</li> <li>A bound whose value is no longer comparable to the old one (for example the   <code>dtype</code> flipped from numeric to <code>timestamp</code> in the same change) is reported as   breaking.</li> <li><code>description</code> and <code>metadata</code> are not compared \u2014 they do not affect validation.</li> </ul>"},{"location":"guide/diff/#json-output","title":"JSON output","text":"<pre><code>dq diff old.yaml new.yaml --output json\n</code></pre> <p>The schema is stable:</p> <pre><code>{\n  \"old_contract\": \"orders\",\n  \"new_contract\": \"orders\",\n  \"summary\": { \"total\": 3, \"breaking\": 1, \"non_breaking\": 2 },\n  \"has_breaking\": true,\n  \"changes\": [\n    {\n      \"kind\": \"field_changed\",\n      \"classification\": \"breaking\",\n      \"reason\": \"stricter lower bound\",\n      \"column\": \"amount\",\n      \"attribute\": \"min\",\n      \"old\": 0,\n      \"new\": 10\n    }\n  ]\n}\n</code></pre> <p><code>kind</code> is one of <code>column_added</code>, <code>column_removed</code>, <code>field_added</code>, <code>field_removed</code>, <code>field_changed</code>, <code>table_rule_added</code>, <code>table_rule_removed</code>, <code>cross_column_rule_added</code>, <code>cross_column_rule_removed</code>, <code>cross_column_rule_changed</code>. <code>column</code> and <code>attribute</code> are <code>null</code> for table-level and cross-column changes. <code>old</code> / <code>new</code> are <code>null</code> when the field or object was absent on that side.</p>"},{"location":"guide/diff/#python-api","title":"Python API","text":"<p><code>dq diff</code> wraps <code>dqflow.diff.diff_contracts()</code>:</p> <pre><code>from dqflow import Contract, diff_contracts\n\nresult = diff_contracts(\n    Contract.from_yaml(\"contracts/orders@v1.yaml\"),\n    Contract.from_yaml(\"contracts/orders@v2.yaml\"),\n)\n\nif result.has_breaking:\n    for change in result.breaking_changes:\n        print(change.column, change.attribute, change.reason)\n\nreport = result.to_dict()  # stable JSON structure\nprint(result.render_text())  # the grouped text summary\n</code></pre> <p><code>diff_contracts()</code> also accepts paths directly: <code>diff_contracts(\"v1.yaml\", \"v2.yaml\")</code>.</p>"},{"location":"guide/diff/#in-ci","title":"In CI","text":"<pre><code>- name: Contract compatibility\n  run: |\n    git show origin/main:contracts/orders.yaml &gt; /tmp/orders-main.yaml\n    dq diff /tmp/orders-main.yaml contracts/orders.yaml\n</code></pre> <p>The step fails when the pull request tightens the contract. Reviewers who have decided a breaking change is acceptable re-run with <code>--allow-breaking</code> or merge past the failed check.</p> <p>See CLI usage for the other <code>dq</code> commands.</p>"},{"location":"guide/editor-integration/","title":"Editor integration","text":"<p>dqflow publishes a JSON Schema for the contract format at a stable URL:</p> <pre><code>https://dqflow.github.io/dqflow/schema/contract-1.0.json\n</code></pre> <p>Point your editor's YAML tooling at it and you get autocompletion, hover docs, and inline flags for obvious mistakes (unknown fields, wrong types) as you type. <code>dq schema</code> prints the same document for offline use.</p> <p>Note</p> <p>The JSON Schema is a subset of what <code>dq lint</code> checks \u2014 it cannot express <code>min</code> \u2264 <code>max</code>, regex validity, or table-rule syntax. Keep <code>dq lint</code> in your pre-commit hook and CI; the schema is for the editing loop.</p>"},{"location":"guide/editor-integration/#per-file-any-editor-with-a-yaml-language-server","title":"Per-file (any editor with a YAML language server)","text":"<p>Add a modeline to the top of the contract:</p> <pre><code># yaml-language-server: $schema=https://dqflow.github.io/dqflow/schema/contract-1.0.json\nschema_version: \"1.0\"\nname: orders\ncolumns:\n  order_id:\n    dtype: string\n    not_null: true\n</code></pre> <p>Or use the schema's own convention \u2014 a <code>$schema</code> key, which dqflow accepts and ignores:</p> <pre><code>$schema: https://dqflow.github.io/dqflow/schema/contract-1.0.json\nschema_version: \"1.0\"\nname: orders\n</code></pre>"},{"location":"guide/editor-integration/#vs-code","title":"VS Code","text":"<p>Install the YAML extension and map your contract files in <code>settings.json</code>:</p> <pre><code>{\n  \"yaml.schemas\": {\n    \"https://dqflow.github.io/dqflow/schema/contract-1.0.json\": [\n      \"contracts/**/*.yaml\",\n      \"**/*.contract.yaml\"\n    ]\n  }\n}\n</code></pre>"},{"location":"guide/editor-integration/#jetbrains-ides-pycharm-intellij","title":"JetBrains IDEs (PyCharm, IntelliJ)","text":"<p>Settings \u2192 Languages &amp; Frameworks \u2192 Schemas and DTDs \u2192 JSON Schema Mappings, add a mapping from the URL above to a file glob such as <code>contracts/*.yaml</code>.</p>"},{"location":"guide/editor-integration/#offline-vendored-schema","title":"Offline / vendored schema","text":"<pre><code>dq schema &gt; contracts/contract.schema.json\n</code></pre> <p>Then point <code>$schema</code> / <code>yaml.schemas</code> at the local path instead of the URL.</p>"},{"location":"guide/engines/","title":"Choosing an engine","text":"<p>dqflow has a stable default pandas engine and an experimental Polars engine. Both produce the same <code>ValidationResult</code> shape and are covered by output-parity tests.</p> Engine Install Input Current behavior pandas <code>pip install dqflow</code> <code>pandas.DataFrame</code> Default and stable Polars <code>pip install \"dqflow[polars]\"</code> <code>DataFrame</code> or <code>LazyFrame</code> Experimental; lazy input is collected"},{"location":"guide/engines/#pandas","title":"Pandas","text":"<p>No engine argument is needed:</p> <pre><code>result = contract.validate(pandas_df)\n</code></pre> <p>Choose pandas for the most established path, CLI file loading, and existing pandas pipelines.</p>"},{"location":"guide/engines/#polars","title":"Polars","text":"<pre><code>result = contract.validate(polars_df, engine=\"polars\")\n\n# or pass an instance\nfrom dqflow.engines.polars import PolarsEngine\n\nresult = contract.validate(polars_df, engine=PolarsEngine())\n</code></pre> <p><code>engine=</code> accepts a registered name (<code>\"pandas\"</code>, <code>\"polars\"</code>), an <code>Engine</code> instance, or <code>None</code> for the pandas default. The CLI exposes the same choice as <code>dq validate --engine polars</code>.</p> <p>Choose Polars when the pipeline already uses Polars and you are comfortable with an experimental API. Passing a <code>LazyFrame</code> does not keep execution lazy today; the engine calls <code>collect()</code> first. Track lazy execution in #25.</p>"},{"location":"guide/engines/#runtime-configuration-with-executioncontext","title":"Runtime configuration with <code>ExecutionContext</code>","text":"<p><code>engine=</code> is a shortcut. The full runtime configuration for a run lives in an <code>ExecutionContext</code> \u2014 a frozen dataclass you pass as <code>context=</code>:</p> <pre><code>from dqflow import ExecutionContext\n\ncontext = ExecutionContext(engine=\"polars\", cache=False)\nresult = contract.validate(polars_df, context=context)\n</code></pre> <p><code>engine=</code> and <code>context=</code> are mutually exclusive.</p> Field Effect today <code>engine</code> Registered engine name; resolved through the registry. <code>cache</code> <code>True</code> (default) memoises table-rule statistics so a column used by several rules is scanned once. <code>False</code> recomputes every statistic on access. <code>parallel</code>, <code>max_workers</code> Carried, no effect yet \u2014 reserved for #22. <code>strict</code>, <code>fail_fast</code> Carried, no effect yet \u2014 reserved for #44. <code>fail_fast</code> here is unrelated to the <code>dq validate --fail-fast</code> exit-code flag. <p>The CLI builds the context for you: <code>dq validate --engine polars</code> runs with <code>ExecutionContext(engine=\"polars\")</code>.</p>"},{"location":"guide/engines/#registering-a-custom-engine","title":"Registering a custom engine","text":"<pre><code>from dqflow.engines import register_engine\n\nregister_engine(\"myengine\", lambda: MyEngine())\nresult = contract.validate(df, engine=\"myengine\")\n</code></pre>"},{"location":"guide/engines/#constraints-shared-by-both-engines","title":"Constraints shared by both engines","text":"<p>Both engines enforce column existence, <code>not_null</code>, <code>min</code>, <code>max</code>, <code>allowed</code>, <code>unique</code>, <code>pattern</code>, table rules, and cross-column rules. Neither currently enforces declared dtype, freshness, or <code>Column.custom</code>.</p> <p>Engines do not read <code>Column</code> objects directly. <code>Contract.validate()</code> first compiles the contract into an engine-agnostic <code>ValidationSpec</code> \u2014 an ordered list of checks \u2014 and every engine executes that same spec. Table-rule expressions run through the shared <code>dqflow.rules</code> evaluator, and their statistics come from a shared lazy <code>StatsCache</code>. A new engine only implements spec execution plus three cache primitives.</p>"},{"location":"guide/inference/","title":"Inferring Contracts","text":"<p><code>dq infer</code> profiles a CSV, JSON, or Parquet file and writes a draft YAML contract. The first two comment lines record the source, number of rows, and inference time, and remind you to review the result before committing it.</p> <pre><code>dq infer data/orders.csv contracts/orders.yaml --sample 100000\n</code></pre> <p>The output preserves source column order and writes each constraint in a stable order, so rerunning inference produces reviewable diffs.</p>"},{"location":"guide/inference/#heuristics","title":"Heuristics","text":"<p>Inference uses only the rows it reads:</p> Constraint Inferred when <code>dtype</code> The pandas dtype maps to <code>boolean</code>, <code>integer</code>, <code>float</code>, <code>timestamp</code>, or <code>string</code> <code>not_null</code> No sampled value is null <code>allowed</code> A string or categorical column has at most 20 distinct non-null values by default <code>min</code> / <code>max</code> A non-empty numeric or datetime column has observed bounds <code>unique</code> All non-null sampled values are distinct <code>pattern</code> Every non-null string matches a supported email, UUID, or ISO <code>YYYY-MM-DD</code> format <p>Nulls do not count as values for <code>allowed</code>, <code>unique</code>, or <code>pattern</code>. Bounds are the exact observed bounds; inference does not add a margin. A contract inferred from a sample describes that sample, not every value that future data may contain.</p>"},{"location":"guide/inference/#tuning-inference","title":"Tuning inference","text":"<pre><code>dq infer DATA OUTPUT \\\n  --sample 100000 \\\n  --max-allowed-cardinality 10 \\\n  --no-ranges \\\n  --strict\n</code></pre> <ul> <li><code>--sample N</code> reads at most the first <code>N</code> rows. Omit it to read the full file.</li> <li><code>--max-allowed-cardinality N</code> changes the <code>allowed</code> threshold. Use <code>0</code> to   disable <code>allowed</code> inference.</li> <li><code>--no-ranges</code> disables numeric and datetime <code>min</code> / <code>max</code> inference.</li> <li><code>--strict</code> makes malformed CSV rows fail the command. Without it, malformed   CSV rows are skipped; other read errors still fail.</li> </ul> <p>Sampling can make identifier-like columns appear unique and can miss rare enum values, nulls, outliers, or malformed formats. Review those constraints before using the generated contract in CI.</p>"},{"location":"guide/inference/#python-api","title":"Python API","text":"<p>Inference is also available without the CLI:</p> <pre><code>import pandas as pd\nfrom dqflow import infer_contract\n\ndf = pd.read_csv(\"data/orders.csv\")\ncontract = infer_contract(\n    df,\n    name=\"orders\",\n    infer_ranges=True,\n    max_allowed_cardinality=20,\n)\nassert contract.validate(df).ok\ncontract.to_yaml(\"contracts/orders.yaml\")\n</code></pre>"},{"location":"guide/lint/","title":"Linting contracts","text":"<p><code>dq lint</code> checks a contract file for schema and structural problems without reading any data. Use it in an editor loop, a pre-commit hook, or a CI step so a broken contract fails fast with a clear location instead of a traceback part-way through validation.</p> <pre><code>dq lint contracts/orders.yaml\n</code></pre> <pre><code>contracts/orders.yaml: 2 errors, 1 warning\n\n  ERROR  columns.amount.min:6   min (100) is greater than max (1)         [min-greater-than-max]\n  ERROR  columns.amount.bogus:8  unknown field 'bogus'; expected one of \u2026  [unknown-field]\n  WARN   (root)                  no schema_version; assuming \"1.0\"         [missing-schema-version]\n</code></pre> <p>Every diagnostic carries a document path (<code>columns.amount.min</code>, <code>rules[2]</code>) and, when the file could be tracked, a line number.</p>"},{"location":"guide/lint/#options-and-exit-codes","title":"Options and exit codes","text":"<pre><code>dq lint CONTRACT [--output text|json] [--strict]\n</code></pre> Exit Meaning <code>0</code> No errors (warnings are allowed unless <code>--strict</code>). <code>1</code> One or more errors \u2014 or, with <code>--strict</code>, one or more warnings. <code>2</code> Usage error (missing file, bad option). <p><code>--output json</code> prints <code>{\"contract\", \"ok\", \"error_count\", \"warning_count\", \"diagnostics\": [...]}</code>, where each diagnostic is <code>{\"severity\", \"code\", \"message\", \"path\", \"line\"}</code>. The <code>code</code> is a stable token you can branch on; the <code>message</code> is not.</p> <p><code>Contract.from_yaml()</code> runs the same checks and raises <code>ContractParseError</code> / <code>ContractSchemaError</code> / <code>ContractVersionError</code> (all subclasses of <code>ContractError</code>); <code>dq validate</code>, <code>dq show</code> and <code>dq diff</code> surface those as a one-line error and point you at <code>dq lint</code>.</p> <p>For the editing loop, dqflow also publishes a JSON Schema \u2014 <code>dq schema</code> prints it \u2014 that a YAML language server can check as you type. It is a subset of the linter (no cross-field checks), so keep <code>dq lint</code> in CI.</p>"},{"location":"guide/lint/#diagnostic-codes","title":"Diagnostic codes","text":""},{"location":"guide/lint/#errors-the-contract-will-not-load","title":"Errors \u2014 the contract will not load","text":"Code Trigger <code>invalid-yaml</code> The file is not valid YAML. <code>not-a-mapping</code> The document, <code>columns</code>, or a column entry is not a mapping. <code>unknown-field</code> A key that is not part of the schema (<code>metadata:</code> is the escape hatch for arbitrary data). <code>wrong-type</code> A value has the wrong type \u2014 <code>not_null</code> not a boolean, <code>rules</code> not a list, and so on. <code>min-greater-than-max</code> A column's <code>min</code> exceeds its <code>max</code>. <code>invalid-regex</code> A <code>pattern</code> is not a valid regular expression. <code>invalid-rule</code> A table rule fails to parse or uses a construct outside the rule whitelist. <code>invalid-operator</code> A cross-column rule <code>op</code> is not one of <code>&gt;= &lt;= &gt; &lt; == !=</code>. <code>incomplete-cross-column-rule</code> A cross-column rule is missing <code>name</code>, or any of <code>left</code> / <code>op</code> / <code>right</code> (callable rules cannot be written in YAML). <code>duplicate-cross-column-rule-name</code> Two cross-column rules share a <code>name</code>. <code>unsupported-schema-version</code> <code>schema_version</code> is a different major version, or not <code>MAJOR.MINOR</code>."},{"location":"guide/lint/#warnings-loadable-but-probably-not-intended","title":"Warnings \u2014 loadable, but probably not intended","text":"Code Trigger <code>missing-schema-version</code> No <code>schema_version</code>; the current version is assumed. <code>newer-schema-minor</code> <code>schema_version</code> is a newer minor of the current major. <code>missing-name</code> No <code>name</code>; the file stem is used. <code>missing-column-dtype</code> A column mapping has no <code>dtype</code> / <code>type</code>; it defaults to string. <code>empty-contract</code> No columns, rules, or cross-column rules. <code>empty-allowed</code> <code>allowed: []</code> \u2014 every non-null value would fail. <code>duplicate-rule</code> The same table-rule string appears twice."},{"location":"guide/rules/","title":"Table rules","text":"<p>Table rules evaluate aggregate properties of the complete DataFrame.</p> <pre><code>from dqflow import Contract\n\ncontract = Contract(\n    name=\"orders\",\n    rules=[\n        \"row_count &gt; 0\",\n        \"null_rate('amount') &lt; 0.01\",\n        \"unique_count('currency') &lt;= 3\",\n    ],\n)\n</code></pre>"},{"location":"guide/rules/#available-names","title":"Available names","text":"Name Result <code>row_count</code> Total rows as an integer <code>null_rate('column')</code> Null proportion from <code>0.0</code> to <code>1.0</code> <code>unique_count('column')</code> Distinct count, including null as a distinct value <p>Column names must be string literals. <code>duplicate_rate</code> and other helpers are not implemented yet.</p> <p>Rules can use ordinary comparisons and boolean operators:</p> <pre><code>\"row_count &gt;= 100\"\n\"row_count &gt; 0 and null_rate('email') &lt; 0.05\"\n\"unique_count('status') &lt;= 10\"\n</code></pre> <p>When a rule evaluates to false, its check fails. Evaluation errors also become failed checks rather than escaping as exceptions.</p>"},{"location":"guide/rules/#safety-model","title":"Safety model","text":"<p>Rule expressions are parsed with <code>ast</code> and evaluated by <code>dqflow.rules.evaluate_rule</code> \u2014 one shared evaluator for every engine \u2014 which walks a strict whitelist of nodes: the three names above, numeric and string literals, <code>and</code> / <code>or</code> / <code>not</code>, arithmetic, and comparisons. There is no <code>eval</code>, no attribute access, no indexing, no builtins, and no calls other than <code>null_rate()</code> / <code>unique_count()</code> with a string-literal column name. Anything else raises and the check fails with <code>Failed to evaluate rule: ...</code>.</p> <p>This is a much smaller surface than <code>eval</code>, but it is still not a security boundary: only run rule expressions from contracts you trust. Do not accept arbitrary YAML contracts from users or third parties.</p> <p>For row-wise relationships and Python callables, use Cross-column and custom checks.</p>"},{"location":"guide/schema-versioning/","title":"Schema versioning","text":"<p>A dqflow contract declares the version of the contract format it is written in:</p> <pre><code>schema_version: \"1.0\"\nname: orders\ncolumns:\n  order_id:\n    dtype: string\n    not_null: true\n</code></pre> <p><code>Contract.to_yaml()</code> and <code>dq infer</code> always write the current version (<code>1.0</code>). The value is <code>MAJOR.MINOR</code>.</p> <p>Each version has a published JSON Schema \u2014 <code>https://dqflow.github.io/dqflow/schema/contract-1.0.json</code> (also <code>dq schema</code>) \u2014 for editor tooling.</p>"},{"location":"guide/schema-versioning/#compatibility-policy","title":"Compatibility policy","text":"<p>When dqflow reads a contract it compares the file's <code>schema_version</code> with the version it emits:</p> File version vs. current Behaviour Same major, minor \u2264 current Loaded normally. Same major, minor &gt; current Loaded, with a <code>newer-schema-minor</code> warning \u2014 the file may use fields this dqflow does not know about. Different major, or not <code>MAJOR.MINOR</code> Rejected with <code>ContractVersionError</code> / an <code>unsupported-schema-version</code> error. Absent Loaded as the current version, with a <code>missing-schema-version</code> warning. Add the key to silence it. <p>In short: a minor bump only adds optional things (older dqflow keeps working, newer files stay readable), and a major bump may remove or redefine fields (and is a hard stop for an older dqflow).</p>"},{"location":"guide/schema-versioning/#deprecation","title":"Deprecation","text":"<p>Before a field is removed or repurposed in a new major version it is deprecated for at least one minor release, called out in the changelog, and \u2014 where the change is mechanical \u2014 an upgrade note is provided. Contracts you have already committed keep loading until the next major version; <code>dq lint</code> tells you when one needs attention.</p>"},{"location":"guide/schema-versioning/#checking-a-contract","title":"Checking a contract","text":"<pre><code>dq lint contracts/orders.yaml\n</code></pre> <p>See Linting contracts for the full diagnostic list.</p>"},{"location":"guide/yaml/","title":"YAML contracts","text":"<p>YAML contracts are declarative files suited to code review and version control.</p> <pre><code>schema_version: \"1.0\"\nname: orders\ndescription: Orders emitted by checkout\n\ncolumns:\n  order_id:\n    dtype: string\n    not_null: true\n    unique: true\n    pattern: \"^A[0-9]{3}$\"\n  amount:\n    dtype: float\n    min: 0\n  currency:\n    dtype: string\n    allowed: [USD, EUR]\n\nrules:\n  - row_count &gt; 0\n  - \"null_rate('amount') &lt; 0.01\"\n\ncross_column_rules:\n  - name: shipped_after_created\n    left: shipped_at\n    op: \"&gt;=\"\n    right: created_at\n    error_message: shipped_at must not precede created_at\n</code></pre> <p>Both <code>dtype</code> and the legacy key <code>type</code> are accepted when reading YAML. <code>to_yaml()</code> writes <code>dtype</code>. Declared dtype and freshness are not currently enforced.</p> <p><code>schema_version</code> declares the contract format version. It is optional when reading (the current version is assumed, with a warning) and always written by <code>to_yaml()</code> and <code>dq infer</code>. See Schema versioning.</p> <p>Unknown keys \u2014 at the top level or on a column \u2014 are an error. Use <code>metadata:</code> for arbitrary data.</p> <p>A <code># yaml-language-server: $schema=\u2026</code> modeline or a <code>$schema</code> key wires the file to the published JSON Schema for editor autocompletion:</p> <pre><code># yaml-language-server: $schema=https://dqflow.github.io/dqflow/schema/contract-1.0.json\nschema_version: \"1.0\"\nname: orders\n</code></pre>"},{"location":"guide/yaml/#check-before-you-run","title":"Check before you run","text":"<pre><code>dq lint contracts/orders.yaml\n</code></pre> <p><code>dq lint</code> validates the file's structure without reading any data and reports the exact path of any problem. <code>Contract.from_yaml()</code> runs the same checks and raises a typed <code>ContractError</code> instead of a traceback. See Linting contracts.</p>"},{"location":"guide/yaml/#load-and-save","title":"Load and save","text":"<pre><code>from dqflow import Contract\n\ncontract = Contract.from_yaml(\"contracts/orders.yaml\")\nresult = contract.validate(df)\ncontract.to_yaml(\"contracts/orders-copy.yaml\")\n</code></pre> <p>Callable cross-column rules and <code>custom</code> callables cannot be represented in YAML. Use a Python contract for those cases.</p>"},{"location":"guide/yaml/#validate-with-the-cli","title":"Validate with the CLI","text":"<pre><code>dq show contracts/orders.yaml\ndq validate contracts/orders.yaml data/orders.csv --fail-fast\n</code></pre> <p>See CLI usage for exit-code and file-format details.</p>"},{"location":"reference/stability/","title":"Stability &amp; compatibility","text":"<p>dqflow is pre-1.0 and still changing quickly. This page defines the small set of surfaces we treat as a compatibility contract, how they may change, and how that promise is enforced.</p> <p>Current status</p> <p>dqflow is <code>0.x</code>. The policy below is already enforced in CI, but the guarantees only become release blocking at 1.0 \u2014 see the 1.0 readiness checklist.</p>"},{"location":"reference/stability/#what-is-covered","title":"What is covered","text":"Surface Specifically Python API Every name exported from <code>dqflow</code> and <code>dqflow.schema</code> (their <code>__all__</code>), and the documented signatures of those functions, classes, and methods. CLI The <code>dq</code> sub-commands, their arguments and options, option names and value types, and defaults. Process exit codes <code>0</code> success, <code>1</code> a contract/data problem the caller asked to fail on, <code>2</code> incorrect invocation. See CLI usage. <code>--output json</code> payloads The key names and structure of <code>dq validate</code>, <code>dq diff</code>, and <code>dq lint</code> JSON output, and of <code>ValidationResult.to_dict()</code> / <code>ContractDiff.to_dict()</code> / <code>Diagnostic.to_dict()</code>. Contract format The YAML fields and the <code>schema_version</code> policy \u2014 governed separately in Schema versioning. <p>A machine-readable description of the first four lives in <code>tests/api_surface/public_surface.json</code>.</p>"},{"location":"reference/stability/#what-is-not-covered","title":"What is not covered","text":"<ul> <li>Any module, class, function, or attribute not re-exported from <code>dqflow</code> /   <code>dqflow.schema</code>, including everything under <code>dqflow.engines</code>, <code>dqflow.report</code>,   <code>dqflow.rules</code>, <code>dqflow.cache</code>, <code>dqflow.execution</code> internals, and any name   starting with <code>_</code>.</li> <li>The Polars engine (<code>dqflow[polars]</code>), which is explicitly experimental   until #25.</li> <li>Human-readable (<code>--output text</code>) CLI output: wording, layout, colour, and the   exact <code>details</code> keys attached to a failing check.</li> <li>Log messages, tracebacks, and the string form of exceptions (their types   are covered).</li> <li>Performance characteristics and memory use.</li> </ul> <p>If you depend on something in this list and need it stabilised, open an issue \u2014 that is how a surface graduates into the table above.</p>"},{"location":"reference/stability/#how-versions-change","title":"How versions change","text":"<p>dqflow follows Semantic Versioning.</p> <p>Before 1.0 (<code>0.y.z</code>)</p> <ul> <li>A <code>0.y.0</code> minor release may make a breaking change to a covered surface.</li> <li>Every such change is listed in the changelog under a   Changed or Removed heading with a one-line migration note.</li> <li>Bug-fix (<code>0.y.z</code>) releases never break a covered surface on purpose.</li> </ul> <p>From 1.0 onwards</p> <ul> <li>Breaking changes to a covered surface happen only in a major release.</li> <li>Minor releases add things (new commands, new optional arguments, new keys in a   JSON payload) without breaking existing use.</li> </ul>"},{"location":"reference/stability/#deprecation-policy","title":"Deprecation policy","text":"<p>Before a covered name, option, or field is removed or repurposed:</p> <ol> <li>It is deprecated for at least one minor release while the old behaviour    keeps working.</li> <li>Using it emits a <code>DeprecationWarning</code> where that is possible (Python API,    CLI).</li> <li>The deprecation and its replacement are called out in    the changelog, with a migration note when the change is    mechanical.</li> </ol> <p>New JSON payload keys can be added at any time, so consumers should ignore keys they do not recognise rather than failing on them.</p>"},{"location":"reference/stability/#how-this-is-enforced","title":"How this is enforced","text":"<p><code>tests/test_public_api.py</code> rebuilds the public surface by introspection on every CI run and compares it to the committed snapshot. Any drift \u2014 a renamed export, a changed signature, a new or removed CLI option, a reshaped JSON payload \u2014 fails the build.</p> <p>A deliberate change is made in three steps:</p> <ol> <li>Make the code change.</li> <li>Add a changelog entry, and update this page if the set of covered surfaces    moved.</li> <li> <p>Regenerate the snapshot so the diff is reviewable:</p> <pre><code>python -m tests.api_surface.collect\n</code></pre> </li> </ol> <p>The snapshot diff in a pull request is the reviewer's signal that a compatibility-relevant change is in play.</p>"},{"location":"reference/v1-readiness/","title":"1.0 readiness checklist","text":"<p>dqflow 1.0 is the point at which the compatibility contract becomes a promise: covered surfaces then change only in a major release. This page is the living gate for that milestone. 1.0 ships when every box is checked.</p> <p>Tracking issue: #67.</p>"},{"location":"reference/v1-readiness/#compatibility-policy","title":"Compatibility policy","text":"<ul> <li>[x] Covered and experimental surfaces are documented \u2014 Stability &amp; compatibility</li> <li>[x] Semantic-versioning and deprecation policies are published, with a minimum notice period</li> <li>[x] CI detects accidental changes to the Python API, CLI, exit codes, and JSON payloads (<code>tests/test_public_api.py</code>)</li> <li>[x] The contract format is versioned with a compatibility policy \u2014 Schema versioning (#61)</li> <li>[ ] Every pre-1.0 breaking change carries an actionable migration note in the changelog (ongoing)</li> <li>[ ] Contract versioning and breaking-change detection for consumers (#41)</li> </ul>"},{"location":"reference/v1-readiness/#correctness-safety","title":"Correctness &amp; safety","text":"<ul> <li>[ ] Declared <code>dtype</code> is enforced by the engines, or removed from the covered contract format</li> <li>[ ] <code>freshness_minutes</code> and <code>custom</code> are enforced, or explicitly marked non-covered</li> <li>[ ] Polars engine reaches pandas parity and graduates from experimental (#25)</li> <li>[ ] Validation-severity levels and failure thresholds are settled (#44)</li> </ul>"},{"location":"reference/v1-readiness/#supported-environments","title":"Supported environments","text":"<ul> <li>[ ] A public compatibility matrix and support policy exist (Python, pandas, Polars, PyArrow) (#65)</li> <li>[ ] CI covers every supported Python version and the min/latest dependency sets (#65)</li> <li>[ ] Windows and macOS smoke tests cover CLI paths and serialization (#65)</li> <li>[ ] Each backend installs with only its documented dependencies (#65)</li> <li>[ ] Built-wheel install/import smoke tests run before every release (#65)</li> </ul>"},{"location":"reference/v1-readiness/#supply-chain-release","title":"Supply chain &amp; release","text":"<ul> <li>[ ] <code>SECURITY.md</code>, pinned GitHub Actions, build provenance / attestations, and an SBOM (#66)</li> <li>[ ] Release checks fail on metadata, wheel-content, or optional-extra drift (#65)</li> </ul>"},{"location":"reference/v1-readiness/#performance","title":"Performance","text":"<ul> <li>[ ] A benchmark suite with committed baselines and a CI regression gate (#45)</li> <li>[ ] A published performance baseline for a representative validation workload</li> </ul>"},{"location":"reference/v1-readiness/#documentation","title":"Documentation","text":"<ul> <li>[ ] Documentation site published with Getting Started, Concepts, Guides, and Reference (#76)</li> <li>[ ] Contract diff is a headline, end-to-end story with a copyable CI gate (#69)</li> <li>[ ] A first-class pytest integration is documented (#64)</li> <li>[ ] An upgrade guide covering every pre-1.0 breaking change</li> </ul>"},{"location":"reference/v1-readiness/#adoption-signal","title":"Adoption signal","text":"<ul> <li>[ ] Adoption metrics and a project-health baseline are in place (#60)</li> <li>[ ] No known unresolved design question about a covered surface</li> </ul>"},{"location":"workflows/ci-pull-request/","title":"Gate a pull request","text":"<p>Store a representative fixture and its YAML contract in the repository, then run the CLI with <code>--fail-fast</code> in CI.</p> <pre><code>name: Data contract\n\non:\n  pull_request:\n\njobs:\n  validate:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n      - run: pip install dqflow\n      - name: Validate fixture\n        run: dq validate contracts/orders.yaml data/orders.csv --fail-fast\n</code></pre> <p>The command evaluates all checks and exits <code>1</code> after printing failures. Use JSON when another step needs to consume the result:</p> <pre><code>dq validate contracts/orders.yaml data/orders.csv --output json --fail-fast\n</code></pre> <p>Fixtures protect the contract and validation wiring; they do not validate live production data. Run the same contract inside the production pipeline as well.</p> <p>See the runnable <code>examples/ci-validation</code>.</p>"},{"location":"workflows/etl-pipeline/","title":"Validate in an ETL pipeline","text":"<p>Validate immediately after loading or transforming data and before writing to a downstream destination.</p> <pre><code>import pandas as pd\nfrom dqflow import Contract\n\n\ndef load_orders() -&gt; pd.DataFrame:\n    return pd.read_csv(\"data/orders.csv\")\n\n\ndef publish_orders(df: pd.DataFrame) -&gt; None:\n    df.to_parquet(\"output/orders.parquet\")\n\n\ndef run() -&gt; None:\n    contract = Contract.from_yaml(\"contracts/orders.yaml\")\n    orders = load_orders()\n    result = contract.validate(orders)\n\n    if not result.ok:\n        # Log the structured form before failing the task.\n        print(result.to_dict())\n        raise RuntimeError(result.summary())\n\n    publish_orders(orders)\n</code></pre> <p>This pattern works in a plain script and in task functions for Airflow, Dagster, or Prefect: raising an exception makes the orchestrator task fail. Do not catch and discard that exception unless the pipeline has an explicit quarantine path.</p> <p>A complete runnable version is in <code>examples/pandas-etl</code>.</p>"},{"location":"workflows/infer-refine/","title":"Infer and refine a contract","text":"<p>Inference is useful for a first draft, but observed data is not automatically a safe specification.</p>"},{"location":"workflows/infer-refine/#1-infer-from-representative-data","title":"1. Infer from representative data","text":"<pre><code>dq infer data/customers.csv contracts/customers.yaml --sample 100000\n</code></pre>"},{"location":"workflows/infer-refine/#2-review-every-observed-constraint","title":"2. Review every observed constraint","text":"<p>Pay particular attention to:</p> <ul> <li><code>unique</code> inferred from a small sample;</li> <li>exact numeric min/max values with no business margin;</li> <li><code>allowed</code> values that may omit rare future categories;</li> <li><code>not_null</code> when the sample happened to contain no nulls;</li> <li>regex patterns inferred from only email, UUID, or ISO-date shapes.</li> </ul> <p>Remove accidental constraints and replace observed ranges with business limits.</p>"},{"location":"workflows/infer-refine/#3-validate-and-commit","title":"3. Validate and commit","text":"<pre><code>dq show contracts/customers.yaml\ndq validate contracts/customers.yaml data/customers.csv --fail-fast\ngit add contracts/customers.yaml\n</code></pre> <p>Review the YAML diff before committing it. Re-run inference deliberately when the source schema changes; do not overwrite a curated contract automatically.</p> <p>See <code>examples/infer-refine</code> for Python API and CLI variants.</p>"}]}